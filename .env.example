# Copy to ".env" and fill in. Used for LOCAL runs and the PC (Task Scheduler) fallback.
# On GitHub Actions these are set as repository *Secrets* instead (never commit real values).

GMAIL_USER=stkotum@gmail.com
GMAIL_APP_PASSWORD=your-16-char-app-password
ALERT_TO=stephan.kohlhaas@tum.de

# Optional:
# STATE_FILE=state.json   # where the baseline is stored
# DRY_RUN=1               # never send email (print only)
